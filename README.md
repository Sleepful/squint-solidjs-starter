From https://github.com/squint-cljs/squint/tree/main/examples/solid-js

# Squint + solid-js example

Run `bb dev` (or look in `bb.edn` what to run). This example uses `bun` but you
can use `npx` instead too.
